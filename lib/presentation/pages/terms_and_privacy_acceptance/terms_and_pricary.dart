import 'package:flutter/material.dart';

class TermsAndPricary extends StatelessWidget {
  const TermsAndPricary({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Terms And Pricary')));
  }
}
